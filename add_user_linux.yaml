---
- name: Créer un utilisateur sur Active Directory
  tasks:
    - name: Add User
      user:
        name: "{{ user }}"
        comment: "{{ user }}"
        password: "{{ password }}"
      no_log: true
