---
- name: Créer un utilisateur sur Linux
  hosts: "{{ hosts }}"
  tasks:
    - name: Add User
      user:
        name: "{{ user }}"
        comment: "{{ user }}"
        password: "{{ password }}"
      no_log: true
